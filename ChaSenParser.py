#!/usr/bin/env python
# -*- coding: utf-8 -*

def ChaSenParser(ChaSenInput):
	lines=ChaSenInput.splitlines(false)
	parsedlines=[]
	for line in lines:
		parsedlines.add(line.split(" "))
	parsedwords=[]
	for word in words:
		parsedwords.add(word[0],word[3])
	return parsedwords
def hinshiStrip(words)
	for word in words:
		if word[1] not in '一般':
			if word[1] not in '自立':
				wordk
	words2=[x for x in words if x[1] not in '一般' or '自立' ]
